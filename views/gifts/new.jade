<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  title Document
body
  h1 Enter a gift from #{guest.name}:
  form(action="/#{guest.id}/gifts", method="POST")
    h4 Description of gift
    input(type='textbox', name="description")
    h4 Link to image of gift <em>*optional*</em>
    input(type='text', name="img")
    br
    input(type='submit', value="submit")